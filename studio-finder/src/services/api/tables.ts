export enum ViewName {
  roles = 'roles'
}

export enum TableName {
  users = 'users',
  studios = 'studios',
  studioUsers = 'studio_users',
  spaces = 'spaces',
  spaceUsers = 'space_users',
  // settings
  settings = 'settings',
}
